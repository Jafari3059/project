#include <iostream>
#include <string>
using namespace std;

// capacity of courses
const int MAX_COURSES = 100;

enum Day {
      SUNDAY,
      MONDAY,
      TUESDAY,
      WEDNESDAY,
      THURSDAY,
      FRIDAY,
      SATURDAY
};

class Time {
      int startingHour;
      int startingMinute;
      int hourDuration;
      int minuteDuration;
      
      public: Time() {
            startingHour = 0;
            startingMinute = 0;
            hourDuration = 0;
            minuteDuration = 0;
      }
      
      public: Time(int startingHour, int startingMinute, int hourDuration, int minuteDuration)
      {
            this->startingHour = startingHour;
            this->startingMinute = startingMinute;
            this->hourDuration = hourDuration;
            this->minuteDuration = minuteDuration;
      }
};

class ClassLocation {
      int classNumber;
      
      public: ClassLocation(int classNumber = 0)
      {
            this->classNumber = classNumber;
      }
};

class Course {
      string id;
    string name;
    string teacher;
    Day day;
    Time time;
    bool hasProjector;
    int studentCapacity;
    int* studentList;
    ClassLocation classLocation;
   
    Course(string id, string name, string teacher, Day day, Time time, bool hasProjector, int studentCapacity, ClassLocation location)
    {
      this->id = id;
      this->name = name;
      this->teacher = teacher;
      this->day = day;
      this->time = time;
      this->hasProjector = hasProjector;
      this->studentCapacity = studentCapacity;
      this->classLocation = classLocation;
      int* studentList = new int[studentCapacity];
      }
};


int main(void){return 0;}

